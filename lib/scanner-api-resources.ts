import {} from 'aws-cdk-lib';
import { Construct } from 'constructs';

export class ScannerApiResources extends Construct {
    constructor(scope: Construct, id: string) {
        super(scope, id);
    }
}