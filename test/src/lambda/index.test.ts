import { handler } from './../../../src/lambda';

describe('lambda scanner function tests', () => {
    beforeEach(() => {
        jest.clearAllMocks();
    })

    test('todo', () => {
        
    })
})